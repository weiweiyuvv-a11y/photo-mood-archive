# 项目约定

用户已明确要求：**UI 恢复原版，只修数据逻辑。**

- 视觉基线是 `prototype-v0.1`；`v0.2.0` 的重设计已被用户否定。
- 未得到用户新的明确要求，不修改布局、配色、字体、导航、心情面板或新增页面。
- 可以修复数据加载、权限同步、持久化、并发、取消和现有视图的数据绑定；测试标识不应改变视觉。
- 保留包名和旧版 v1 存储键，避免覆盖安装时丢失手账。
- PhotoKit 写操作只在独立测试模拟器的样例照片上验证，不使用个人真实图库。
